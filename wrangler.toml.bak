# Cloudflare Pages configuration
name = "singaporehandpans"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Build configuration - environment variables are inherited from Cloudflare Pages dashboard
# Make sure to set STORYBLOK_TOKEN in Cloudflare Pages > Settings > Environment Variables

[vars]
NODE_ENV = "production"

